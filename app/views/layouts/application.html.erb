<!DOCTYPE html>
<html>
  <head>
    <title>Railspractice</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag(
                            content_for(:main_stylesheet) || "application",
                            media: content_for(:stylesheet_media) || "screen")
                            %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
